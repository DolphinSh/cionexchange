spring:
  application:
    name: finance-service
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: nacos-server:8848
      config:
        file-extension: yaml
        #去nacos-server 拉取配置文件finance-service-dev.yaml
